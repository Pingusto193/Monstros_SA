# Multiplicador da latência simulada dos serviços mockados.
# 1 = latência realista (padrão) · 0 = respostas instantâneas.
VITE_MOCK_LATENCY=1

# Futuro: URL da API real (Back_end). Ainda não utilizada nesta etapa.
# VITE_API_URL=http://localhost:3333
